# ─────────────────────────────────────────────
# 行尾归一：入库与检出一律 LF
# ─────────────────────────────────────────────
# 背景：迁移脚本用 Python 的 read_text()/write_text() 读写会静默把 CRLF 转成 LF。
# 这个坑在 2026-09-19 一天内踩了三次（两次是自动化脚本，一次是我自己的清理脚本），
# 每次都在真实改动里埋进上千行幽灵 diff，只能靠
#     git show HEAD:<f> | grep -cU $'\r'
# 逐文件比对行尾数再手工还原。让 git 统一归一化即可从根上消除这类噪声。
#
# ⚠️ 本文件生效后，第一次 `git add --renormalize .` 会重写所有原本是 CRLF 的文件
#    （本次 14 个）。因此它必须**单独成一个提交**，不要和功能改动混在一起，
#    否则功能 diff 会被行尾改动完全淹没。
* text=auto eol=lf

# 二进制：显式标注，绝不参与行尾转换
# （git 的 text=auto 能自动识别，这里只是把意图写死，防止以后加了新格式被误判）
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.webp binary
*.ico  binary
*.woff  binary
*.woff2 binary
